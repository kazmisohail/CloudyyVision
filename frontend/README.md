# CloudVision Frontend 🖥️

This is the user interface for the **CloudVision** satellite analysis platform. It is built with **Next.js 15**, **React**, and **Tailwind CSS** to provide a modern, responsive, and interactive experience.

## ✨ Features

*   **Drag & Drop Upload**: Easy image uploading interface.
*   **Real-time Analysis**: Instant feedback from the backend API.
*   **Interactive Visualizations**:
    *   Side-by-side comparison of Original vs. Mitigated images.
    *   Grayscale Segmentation Masks.
    *   Class Distribution Charts (Chart.js).
*   **AI Integration**: Displays meteorological reports generated by Google Gemini.
*   **Model Selection**: Dropdown to switch between V1, V2, and V3 models.

## 🚀 Getting Started

### Prerequisites
*   Node.js 18+ installed.

### Installation

1.  Navigate to the frontend directory:
    ```bash
    cd frontend
    ```

2.  Install dependencies:
    ```bash
    npm install
    # or
    yarn install
    ```

### Running Locally

Start the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

## 📁 Project Structure

*   `src/app/page.tsx`: Main dashboard page.
*   `src/components/`: Reusable UI components (UploadZone, ResultsDashboard, etc.).
*   `public/`: Static assets.

## 🔧 Configuration

The frontend communicates with the backend at `http://localhost:8000`. Ensure the Django backend is running before using the application.
